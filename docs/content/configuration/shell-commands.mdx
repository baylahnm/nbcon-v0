---
title: Shell Commands
description: Configuring shell commands
lang: en
---

## Shell Commands

Configure custom shell commands for NBCON PRO.

### Built-in Commands

```bash
nbcon run test
nbcon run build
nbcon run deploy
```

### Custom Commands

Add custom commands in `.nbcon/commands.yml`:

```yaml
commands:
  - name: test
    script: npm test
  - name: deploy
    script: npm run deploy
```

### Running Commands

Use the command palette (`Cmd/Ctrl + K`) to run commands.

### Command Aliases

Create aliases for frequently used commands:

```bash
nbcon alias deploy "npm run deploy:production"
```

